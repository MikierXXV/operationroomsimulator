{
  "name": "operationroomsimulator",
  "version": "0.1.0",
  "description": "Simulador de quirÃ³fano en el navegador: prepara la mesa del instrumentista con las manos (webcam) o el ratÃ³n.",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/copiar-wasm.mjs",
    "wasm": "node scripts/copiar-wasm.mjs",
    "dev": "node scripts/copiar-wasm.mjs && vite",
    "build": "node scripts/copiar-wasm.mjs && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.14",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
